{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "zig-test-bin",
            "command": "zig",
            "args": [
                "build",
                "-Dtest-debug-root=${file}"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "type": "shell"
        },
    ]
}